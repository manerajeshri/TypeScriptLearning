// npx tsc --init
// JSON : JavaScript Object Notation 

// API == 

function add(first: string, second: string){
    return second + first;
}

add(1 , 2);

add('hello', 'word'); 

let empId : number   = 101;

empId = null;